<% iam_policies_map().each do |name, policy| -%>
resource "aws_iam_policy_attachment" "<%= name %>-attachments" {
    name       = "<%= name %>-attach"
    users      = [<%= policy[:user].collect{|user| "\"#{user}\""}.join(',') %>]
    roles      = [<%= policy[:role].collect{|role| "\"#{role}\""}.join(',') %>]
    groups     = [<%= policy[:group].collect{|group| "\"#{group}\""}.join(',') %>]
    policy_arn = "<%= policy[:policy_arn] %>"
}

<% end -%>
